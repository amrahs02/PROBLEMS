#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, i, j;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    int arr2[n];
    int j = 0;
    i = 0;
    if (arr[i] != arr[i])
        arr2[j] = arr[i];
    j++;
}